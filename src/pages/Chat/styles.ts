import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 0
    },
    texto: {
        textAlign: 'center',
        alignSelf: 'center'
    }
})

export default styles;